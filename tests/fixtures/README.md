# Fixtures

`komoot_public_smarttour_33303609.payload.json` is a sanitized fixture captured
from the public README example URL on 2026-05-03. It keeps the real tour name and
coordinate items, but omits unrelated page data from the original HTML payload.
